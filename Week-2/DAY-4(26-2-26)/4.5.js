/*Given Data:
                const order = {
                  orderId: "ORD1001",
                  customer: {
                    name: "Anita",
                    address: {
                      city: "Hyderabad",
                      pincode: 500085
                    }
                  },
                  items: [
                    { product: "Laptop", price: 70000 }
                  ]
                };

🎯 Task:
      1. Create a deep copy of order
      2. Modify in copied object:
            i. customer.address.city
            ii. items[0].price
            iii. Verify original object remains unchanged*/
            
    const order = {
                  orderId: "ORD1001",
                  customer: {
                    name: "Anita",
                    address: {
                      city: "Hyderabad",
                      pincode: 500085
                    }
                  },
                  items: [
                    { product: "Laptop", price: 70000 }
                  ]
                };

// Create a deep copy of the order object so nested objects are copied independently
copyorder=structuredClone(order)

// Modify nested properties in the copied object (original remains unchanged)
copyorder.customer.address.city="chennai"
copyorder.items[0].price=60000

// Print both objects to verify that changes in copy do not affect the original
console.log(order)
console.log(copyorder)